{
  "name": "GhostyCode",
  "build": {
    "context": "..",
    "dockerfile": "Dockerfile",
    "args": {
      "RUST_VERSION": "1.88"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "tamasfe.even-better-toml",
        "vadimcn.vscode-lldb"
      ],
      "settings": {
        "rust-analyzer.cargo.features": "all",
        "editor.formatOnSave": true
      }
    }
  },
  "remoteEnv": {
    "DEEPSEEK_API_KEY": "${localEnv:DEEPSEEK_API_KEY}",
    "CARGO_TARGET_DIR": "/home/ghosty/.cargo/target"
  },
  "mounts": [
    "source=ghosty-state,target=/home/ghosty/.ghosty,type=volume",
    "source=ghosty-cargo-target,target=/home/ghosty/.cargo/target,type=volume"
  ],
  "postCreateCommand": "cargo build --locked",
  "remoteUser": "ghosty"
}
